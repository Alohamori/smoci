This is the code that powers the [So Much Only Connect Information](https://prettygr.im/smoci/) Datasette instance. Running it should be as straightforward as:

    datasette --load-extension=extensions/stats .
